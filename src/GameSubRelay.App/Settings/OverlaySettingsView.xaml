<UserControl x:Class="GameSubRelay.App.Settings.OverlaySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GameSubRelay.App"
             Margin="12">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="字幕浮层" FontWeight="SemiBold" FontSize="16" />

            <StackPanel Orientation="Horizontal" Margin="0 0 0 12">
                <TextBlock Text="透明度" Width="80" VerticalAlignment="Center" />
                <Slider Width="260" Minimum="0.2" Maximum="0.95" SmallChange="0.01" LargeChange="0.1"
                        Value="{Binding Opacity, Mode=TwoWay}" />
                <TextBlock Width="48" Text="{Binding Opacity, StringFormat={}{0:P0}}" VerticalAlignment="Center" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 12">
                <TextBlock Text="字号" Width="80" VerticalAlignment="Center" />
                <Slider Width="260" Minimum="14" Maximum="40" SmallChange="1" LargeChange="2"
                        Value="{Binding FontSize, Mode=TwoWay}" />
                <TextBlock Width="48" Text="{Binding FontSize, StringFormat={}{0:0}}" VerticalAlignment="Center" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 12">
                <TextBlock Text="最大行数" Width="80" VerticalAlignment="Center" />
                <TextBox Width="120" Text="{Binding MaxLines}" />
            </StackPanel>

            <TextBlock Text="初始位置和大小" FontWeight="SemiBold" Margin="0 10 0 0" />
            <UniformGrid Columns="2" Rows="2" HorizontalAlignment="Left" Margin="0 4 0 0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="左" Width="80" />
                    <TextBox Width="120" Text="{Binding Left}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="上" Width="80" />
                    <TextBox Width="120" Text="{Binding Top}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="宽" Width="80" />
                    <TextBox Width="120" Text="{Binding Width}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="高" Width="80" />
                    <TextBox Width="120" Text="{Binding Height}" />
                </StackPanel>
            </UniformGrid>

            <TextBlock Text="预览模式会根据设置立即生效（不需要重启）。" Foreground="Gray" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
