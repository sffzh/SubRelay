<Window x:Class="GameSubRelay.App.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GameSubRelay.App"
        Title="GameSubRelay Overlay"
        WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        Background="Transparent"
        AllowsTransparency="True"
        Left="{Binding Settings.Left, Mode=TwoWay}"
        Top="{Binding Settings.Top, Mode=TwoWay}"
        Width="{Binding Settings.Width, Mode=TwoWay}"
        Height="{Binding Settings.Height, Mode=TwoWay}"
        Opacity="{Binding Settings.Opacity}"
        IsHitTestVisible="True">
    <Window.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    <Grid x:Name="Root"
          Background="#A6000000">
        <Border x:Name="ChromeBorder"
                BorderBrush="#7FFFFFFF"
                BorderThickness="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Thickness}"
                Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                   Text="字幕浮层已解锁：拖动窗口移动，拖动右下角调整大小"
                           Foreground="#CCFFFFFF"
                           FontWeight="SemiBold"
                           Margin="0 0 0 8"
                           Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <ItemsControl Grid.Row="1" ItemsSource="{Binding Captions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#40FFFFFF" BorderThickness="0 0 0 1" Padding="4" Margin="0 0 0 6">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding ChannelLabel}" Foreground="#D0FFFFFF" FontSize="14" Margin="0 0 8 0" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SourceText}" Foreground="#A6FFFFFF" FontSize="14" TextWrapping="Wrap" />
                                    <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding TranslatedText}" Foreground="White" FontSize="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.Settings.FontSize}" TextWrapping="Wrap" />
                                    <TextBlock Grid.Row="1"
                                               Grid.ColumnSpan="2"
                                               Text="(interim)"
                                               FontSize="12"
                                               Foreground="#B3FFFFFF"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Bottom"
                                               Visibility="{Binding IsFinal, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Canvas HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="16" Height="16"
                        Margin="0 0 0 0"
                        Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Rectangle x:Name="ResizeGrip"
                               Width="16"
                               Height="16"
                               Fill="#CCFFFFFF"
                               RadiusX="2"
                               RadiusY="2"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Bottom"
                               Cursor="SizeNWSE" />
                </Canvas>
            </Grid>
        </Border>
    </Grid>
</Window>
